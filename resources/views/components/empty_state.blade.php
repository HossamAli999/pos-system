<div class="empty-state">
    <i class="{{ $icon ?? 'fas fa-inbox' }} empty-state__icon" aria-hidden="true"></i>
    <p class="empty-state__text">{{ $text ?? __('purchase.no_records_found') }}</p>
</div>
